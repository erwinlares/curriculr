% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/typst-render.R
\name{cv_render_section}
\alias{cv_render_section}
\title{Render a CV section from a data frame}
\usage{
cv_render_section(
  data,
  title_col,
  org_col = NULL,
  detail_col = NULL,
  date_fun = .cv_date_range,
  where_col = "where"
)
}
\arguments{
\item{data}{A data frame containing CV entries. Typically one element of the
list returned by read_cv_data(), e.g. \code{cv$experience}.}

\item{title_col}{A character string. Name of the column to use as the entry
title. Required.}

\item{org_col}{A character string or \code{NULL}. Name of the column to use as
the organization or secondary text. Defaults to \code{NULL}.}

\item{detail_col}{A character string or \code{NULL}. Name of the column to use as
additional detail. Defaults to \code{NULL}.}

\item{date_fun}{A function or \code{NULL}. Called with each row to produce the
date string. Defaults to \code{.cv_date_range()}. Pass \code{NULL} for sections
without dates.}

\item{where_col}{A character string or \code{NULL}. Name of the column to use as
the location. Defaults to \code{"where"}. Pass \code{NULL} to omit location.}
}
\value{
Invisibly returns \code{NULL}. Called for its side effect of writing
Typst blocks to the Quarto document output stream.
}
\description{
Iterates over a CV data frame and writes each row as a Typst CV entry by
calling \code{.cv_entry()} for each row and passing the result to \code{\link[base:cat]{base::cat()}}.
}
\details{
This function is intended to be called inside a Quarto document chunk with
\code{results = 'asis'}. The \code{cat()} call writes raw Typst blocks directly into
the document output stream. Nothing is returned — the function is called
entirely for its side effect.

For sections that use dates, pass one of \code{.cv_date_range()} or
\code{.cv_year_range()} as \code{date_fun}. For sections where dates are not relevant
(skills, affiliations), pass \code{date_fun = NULL}.
}
\examples{
\dontrun{
# Inside a Quarto chunk with results = 'asis':

# Full entry with date range and location
cat(.cv_section("Experience"))
cv_render_section(cv$experience,
                  title_col  = "title",
                  org_col    = "unit",
                  detail_col = "detail")

# Year-only dates
cat(.cv_section("Education"))
cv_render_section(cv$education,
                  title_col  = "title",
                  org_col    = "institution",
                  detail_col = "detail",
                  date_fun   = .cv_year_range)

# No dates, no location
cat(.cv_section("Skills"))
cv_render_section(cv$skills,
                  title_col = "title",
                  org_col   = "unit",
                  date_fun  = NULL,
                  where_col = NULL)

# Custom inline date function
cat(.cv_section("Presentations"))
cv_render_section(cv$presentations,
                  title_col = "unit",
                  org_col   = "title",
                  date_fun  = function(row) {
                    trimws(paste(.cv_value(row, "startMonth"),
                                 .cv_value(row, "startYear")))
                  })
}

}
