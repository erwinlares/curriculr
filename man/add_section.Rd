% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add-section.R
\name{add_section}
\alias{add_section}
\title{Add a new section to a curriculr workbook}
\usage{
add_section(
  workbook,
  section,
  label = section,
  date_fun = "year_only",
  title_col = "title",
  org_col = "unit",
  detail_col = NA,
  where_col = "where",
  overwrite = FALSE
)
}
\arguments{
\item{workbook}{A character string. Path to an existing curriculr Excel
workbook.}

\item{section}{A character string. Internal name of the new section. Must
be a valid Excel sheet name (no more than 31 characters, no special
characters). This name must match the sheet name exactly when referenced
elsewhere in the workbook or in \code{cap} arguments to \code{\link[=create_cv]{create_cv()}}.}

\item{label}{A character string. Display label shown as the section heading
in the rendered CV. Defaults to \code{section}, which works when the section
name is already human-readable. Supply a different value when the internal
name and the display label differ, e.g. \code{section = "invited_talks"},
\code{label = "Invited Talks"}.}

\item{date_fun}{A character string. Token controlling date formatting for
this section. One of \code{"date"}, \code{"year"}, \code{"month_year"}, \code{"year_only"},
or \code{"none"}. Defaults to \code{"year_only"}. See \code{\link[=resolve_date_fun]{resolve_date_fun()}} for
token definitions.}

\item{title_col}{A character string. Name of the column used as the primary
entry label in the rendered CV. Defaults to \code{"title"}.}

\item{org_col}{A character string or \code{NA}. Name of the column used as the
secondary organization or venue line. Defaults to \code{"unit"}. Pass \code{NA} to
omit the organization line for this section.}

\item{detail_col}{A character string or \code{NA}. Name of the column used as
the detail line. Defaults to \code{NA} (omitted). Pass a column name to
include a detail line.}

\item{where_col}{A character string or \code{NA}. Name of the column used as the
location. Defaults to \code{"where"}. Pass \code{NA} to omit location for this
section.}

\item{overwrite}{A logical. Whether to overwrite an existing sheet of the
same name. Defaults to \code{FALSE}. When \code{FALSE}, an existing sheet causes
an informative error. This is a destructive operation — use with care.}
}
\value{
Invisibly returns \code{workbook}. Called primarily for its side effect
of modifying the workbook on disk.
}
\description{
Adds a new sheet to an existing curriculr-formatted Excel workbook and
registers it in the \code{sections} control sheet. The new sheet is pre-populated
with the standard column spine so the user can start entering data
immediately without worrying about column names.
}
\details{
\code{add_section()} performs the following steps:
\enumerate{
\item Validates that \code{workbook} exists and that \code{section} is not already
present (unless \code{overwrite = TRUE}).
\item Appends a new sheet named \code{section} with the standard column spine:
\code{title | unit | startMonth | startYear | endMonth | endYear | where | detail | include_in_resume}.
\item Appends a new row to the \code{sections} sheet registering the new section
with the supplied metadata. When \code{overwrite = TRUE}, any existing row
for this section is replaced.
\item Writes the modified workbook back to \code{workbook} in place.
}

The workbook is modified in place. There is no undo. Consider keeping a
backup copy before calling \code{add_section()} if the workbook contains data
you cannot reconstruct.

Control sheets (\code{profile}, \code{sections}, \code{theme}, \code{readme}) cannot be used
as section names.
}
\examples{
\dontrun{
# Add a section with defaults
add_section("cv-data.xlsx", section = "patents")

# Add a section with a display label that differs from the sheet name
add_section("cv-data.xlsx",
            section  = "invited_talks",
            label    = "Invited Talks",
            date_fun = "month_year")

# Add a section without an organization or location line
add_section("cv-data.xlsx",
            section   = "languages",
            label     = "Languages",
            date_fun  = "none",
            org_col   = NA,
            where_col = NA)
}
}
