% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/typst-layout.R
\name{.cv_entry}
\alias{.cv_entry}
\title{Create a Typst CV entry}
\usage{
.cv_entry(title = "", organization = "", detail = "", when = "", where = "")
}
\arguments{
\item{title}{A character string. The main entry label: degree, job title,
project name, publication title, skill area, etc.}

\item{organization}{A character string. The secondary line: employer,
institution, publisher, or venue. Defaults to \code{""}.}

\item{detail}{A character string. Additional context shown after the
organization line. Defaults to \code{""}.}

\item{when}{A character string. The date or date range, typically produced
by \code{.cv_date_range()} or \code{.cv_year_range()}. Defaults to \code{""}.}

\item{where}{A character string. Location associated with the entry.
Defaults to \code{""}.}
}
\value{
A character string of raw Typst markup.
}
\description{
Generates a raw Typst block for one CV entry. The entry is laid out as a
two-column grid: the left column holds the title and metadata; the right
column holds the date and location, right-aligned in the accent color.
}
\details{
All arguments are optional except \code{title}. Empty strings are handled
gracefully — missing metadata, dates, or locations are simply omitted from
the rendered output rather than leaving blank space.
}
\keyword{internal}
